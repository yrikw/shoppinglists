require_relative ('../index')

rspec.describe menu4 do
        number = number.new(6).to_i
        person = person.new(2).to_i
      end
        it "should dvide the number" do
        expect(number.to_i / person.to_i).to eq '3'
end
end